<table class="index sortable" id='listing_taxonomies' data-hook data-sortable-link="<%= update_positions_admin_blendable_taxons_url %>">
  <colgroup>
    <col style="width: 85%">
    <col style="width: 15%">
  </colgroup>
  <thead>
    <tr data-hook="taxonomies_header">
      <th><%= t(:name) %></th>
      <th class="actions"></th>
    </tr>
  </thead>
  <tbody>
    <% @blendabletaxons.each do |taxon| %>
      <tr id="<%= spree_dom_id taxon %>" data-hook="taxonomies_row" class="<%= cycle('odd', 'even')%>">
        <td>
          <span class="handle"></span>
          <%= taxon.name %>
        </td>
        <td class="actions">
          <%= link_to_edit taxon.id, :no_text => true %>
          <%= link_to_delete taxon, :no_text => true %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>